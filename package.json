{
  "name": "prism-extension",
  "version": "1.0.0",
  "description": "Prism — refract your prompts into clarity. Analyzes and optimizes your AI prompts for ChatGPT, Claude, Gemini, DeepSeek and Perplexity.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "build": "node scripts/build.js",
    "pack": "node scripts/build.js && node scripts/pack.js && node scripts/verify-zip.js",
    "test": "node tests/diagnostic.js",
    "verify": "npm run icons && npm run build && npm test && node --check background.js && node --check content.js && node --check utils/storage.js && node --check utils/promptAnalyzer.js && node --check utils/templateManager.js && node --check utils/api.js && node --check popup/popup.js && node --check options/options.js && node --check scripts/build.js && node --check scripts/pack.js && node --check scripts/generate-icons.js && echo VERIFY_OK"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
